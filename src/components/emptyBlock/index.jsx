import React from "react";
import styles from "../emptyBlock/EmptyBlock.module.scss";

export const EmptyBlock = () => {
  return <h1 className={styles.root}>Ничего не найдено!</h1>;
};

export default EmptyBlock;
